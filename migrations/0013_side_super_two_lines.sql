-- サイドスーパーを必ず2行（見出し・本文）にする
--
-- side_supers は「1行だけのサイドスーパーでは line2 に空文字を入れる」という作りだったが、
-- テレビのサイドスーパーと同じく上段＝コーナー名・下段＝いまの話題という役割に分けたため、
-- 行数は常に2行になった（worker/side-super.ts の SIDE_SUPER_LINES）。
--
-- 貯めてある行をすべて消す。本文の無い行が残っているとオーバーレイ（src/side-super/view.ts）が
-- 「2行でも0行でもない」として投げてしまい、2行ある行も役割を分ける前の指示で作られたもので、
-- 上段がコーナー名になっていないためである。
--
-- サイドスーパーは配信の区切りごとの一時的な文言で、cron（worker/collect.ts）が5分おきに
-- 作り直すので、消しても作り直される（配信中に適用した場合、次の収集までのあいだ何も映らない）。
DELETE FROM side_supers;
